{
  "name": "dz-rapport-technique-cockpit",
  "version": "2.2.0",
  "description": "Hub Rapport Technique DZ Construct — cockpit de pilotage, dépôt des rapports et conversion Word",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "typecheck": "bash scripts/syntaxe.sh",
    "lint": "eslint . --max-warnings 0",
    "test": "node --test tests/*.test.js",
    "check": "npm run typecheck && npm run lint && npm run test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "express": "^4.19.2",
    "html-to-docx": "^1.8.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.11.0",
    "globals": "^17.12.0"
  }
}
