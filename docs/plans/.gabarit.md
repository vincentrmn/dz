# Plan : <titre en une ligne>

Écrit le <JJ/MM/AAAA> par `/cadrer`, validé par Vincent le <JJ/MM/AAAA>.
Un plan tient sur une page. Il dit ce qu'on construit, ce qu'on ne construit
pas, et comment on saura que c'est fini. Rien ici n'est du code.

## Contexte

<Trois à six lignes : le besoin, d'où il vient, ce qui existe déjà dans le
code (fichiers nommés), les décisions de `docs/decisions.md` qui s'appliquent.>

## Décision

<L'option retenue et pourquoi, en quelques lignes. Les options écartées en
une ligne chacune, avec la raison.>

## Périmètre

- <ce qui change, fichier par fichier ou zone par zone>

## Hors périmètre

- <ce qu'on ne touche pas, même si c'est tentant, et pourquoi>

## Critères vérifiables

Chaque critère démarre à « échec » et ne passe à « réussi » qu'avec une preuve
(la sortie d'une commande, une capture, une réponse HTTP). Un critère sans
commande ni capture n'est pas un critère.

La colonne « État » est tenue par `/construire` : `échec` au départ,
`réussi : <preuve>` quand la preuve a été vue, `bloqué : <raison>` sinon.

| # | Critère | Preuve attendue | État |
|---|---|---|---|
| 1 | <comportement observable> | `<commande>` sort 0 / capture `<nom>.png` à 1440 px et 390 px / `GET <route>` répond <code> | échec |
| 2 | Aucun scroll horizontal, aucune erreur de page sur les pages touchées | `npm run e2e` PASS | échec |
| 3 | `npm run check` sort 0 | sortie de `check` | échec |

## Lots

Un lot = une session `/construire`. Si tout tient dans une session, un seul lot.

1. **Lot 1** : critères <n> à <m>.
2. **Lot 2** : critères <n> à <m>.

## Vérification de bout en bout

<Ce que Vincent teste au clic sur le staging avant `/livrer`, en trois à
cinq lignes : la page, le compte, ce qu'il doit voir.>

## Questions ouvertes

- <une question que seul Vincent peut trancher, ou « aucune »>
